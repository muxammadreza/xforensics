

# 🕵️‍♂️ X Profile Forensics (ابزار تحلیلگر پروفایل ایکس)

**نسخه ۱۳.۰ (Version 13.0.0)**

این یک ابزار رایگان و قدرتمند برای مرورگر است که اطلاعات مخفی و فنی حساب‌های کاربری در **ایکس (توییتر سابق)** را به شما نشان می‌دهد. با این ابزار می‌توانید بفهمید یک کاربر واقعاً از کدام کشور توییت می‌کند، از چه دستگاهی استفاده می‌کند و آیا از VPN استفاده کرده است یا خیر.

این ابزار کاملاً امن است و روی **کامپیوتر (ویندوز/مک)** و **موبایل (اندروید/آیفون)** کار می‌کند.

---

## ✨ این ابزار چه کاری انجام می‌دهد؟

به محض نصب، گزینه‌های جدیدی به توییتر شما اضافه می‌شود:

### ۱. 📍 کشف لوکیشن و دستگاه واقعی
در بالای پروفایل هر کاربر، کادری نمایش داده می‌شود که شامل موارد زیر است:
*   **کشور (Location):** کشوری که اکانت در آن ثبت شده است.
*   **دستگاه (Device):** نوع گوشی (آیفون یا اندروید) و اینکه برنامه توییتر را از اپ‌استور کدام کشور دانلود کرده است.
*   **تاریخ دقیق:** زمان دقیق ساخت اکانت (حتی ساعت و ثانیه).
*   **تغییر نام:** اگر کاربر قبلاً نام کاربری (Handle) خود را تغییر داده باشد، تعداد دفعات آن را می‌بینید.
*   **شناسه ثابت (Perm ID):** یک عدد ثابت که حتی اگر کاربر نامش را عوض کند، تغییر نمی‌کند (برای ردیابی عالی است).

### ۲. 🛡️ تشخیص امنیت و دروغ‌سنجی
*   **🛡️ سپر فعال (Shield Active):** یعنی کاربر از **VPN یا پروکسی** استفاده می‌کند و لوکیشن واقعی او مخفی شده است.
*   **✅ اتصال مستقیم (High Confidence):** یعنی کاربر مستقیماً و بدون واسطه وصل شده است.
*   **⚠️ ناهنجاری (Anomaly - مخصوص ایران):** اگر کاربری لوکیشن «ایران» یا «غرب آسیا» داشته باشد اما اتصالش «مستقیم» باشد (در حالی که توییتر فیلتر است)، این ابزار هشدار می‌دهد که کاربر احتمالا از **سیم‌کارت خاص (سفید)**، **اینترنت بدون فیلتر** یا ترفندهای خاص استفاده می‌کند.

### ۳. 📂 پایگاه داده شخصی (Dashboard)
این ابزار تمام کاربرانی را که بررسی می‌کنید در حافظه مرورگر ذخیره می‌کند. با کلیک روی دکمه **📂 DB** در گوشه صفحه می‌توانید:
*   لیست تمام کاربرانی که چک کرده‌اید را ببینید.
*   از لیست خروجی اکسل (CSV) بگیرید.
*   دیتابیس خود را با دیگران به اشتراک بگذارید.
*   **☁️ دیتابیس ابری:** با زدن یک دکمه، دیتابیس خود را با لیستی از هزاران کاربر شناسایی‌شده توسط جامعه بروزرسانی کنید.

---

## 🚀 راهنمای نصب آسان (مرحله به مرحله)

### 💻 نصب روی کامپیوتر (ویندوز / مک / لینوکس)
1.  روی مرورگر خود (Chrome, Edge, Firefox, Brave) افزونه **Tampermonkey** را نصب کنید.
    *   [دانلود برای کروم/اج](https://chrome.google.com/webstore/detail/tampermonkey/dhdgffkkebhmkfjojejmpbldmpobfkfo)
2.  روی آیکون Tampermonkey در بالای مرورگر کلیک کرده و **Create a new script** را بزنید.
3.  تمام کدهای موجود در صفحه باز شده را پاک کنید.
4.  کد فایل `xforensics.js` را کپی کرده و در آنجا پیست (Paste) کنید.
5.  از منوی بالا گزینه **File** و سپس **Save** را بزنید.
6.  وارد سایت `x.com` شوید و از ابزار لذت ببرید!

### 🤖 نصب روی اندروید (Android)
برنامه رسمی ایکس از این ابزار پشتیبانی نمی‌کند. باید از مرورگر استفاده کنید:
1.  مرورگر **Kiwi Browser** را از گوگل پلی دانلود و نصب کنید.
2.  در مرورگر Kiwi، عبارت "Tampermonkey Extension" را جستجو و نصب کنید.
3.  سپس مانند روش کامپیوتر (بالا)، کد را داخل افزونه کپی و ذخیره کنید.
4.  با مرورگر Kiwi وارد `x.com` شوید.

### 🍎 نصب روی آیفون (iOS)
1.  برنامه رایگان **Userscripts** را از اپ‌استور دانلود کنید.
2.  وارد `Settings` آیفون شوید، به بخش `Apps > Safari > Extensions` بروید و **Userscripts** را روشن کنید.
3.  برنامه Userscripts را باز کنید و یک پوشه در فایل‌های گوشی انتخاب کنید (Set Directory).
4.  در آن پوشه، یک فایل متنی بسازید (مثلاً `script.js`) و کد را داخل آن کپی کنید.
5.  مرورگر سافاری را باز کنید، وارد `x.com` شوید و روی علامت **آأ (aA)** در نوار آدرس بزنید تا افزونه فعال شود.

---

## 📖 راهنمای استفاده

### در پروفایل کاربر
*   بالای صفحه (کنار تاریخ عضویت) یک کادر رنگی می‌بینید.
*   **در کامپیوتر:** موس را روی کادر ببرید تا جزئیات باز شود.
*   **در موبایل:** روی کادر ضربه بزنید تا پنجره جزئیات باز شود.
*   **دکمه ↻ (Refresh):** اگر اطلاعات قدیمی بود، این دکمه را بزنید تا دوباره چک شود.

### در تایم‌لاین (Timeline)
*   کنار اسم کاربران، یک علامت هدف (⌖) کوچک می‌بینید.
*   روی آن ضربه بزنید (یا موس را نگه دارید) تا بدون باز کردن پروفایل، لوکیشن کاربر چک شود.

### مشارکت در دیتابیس (جدید در نسخه ۱۳)
اگر کاربران زیادی را شناسایی کردید، می‌توانید به جامعه کمک کنید:
1.  دکمه **📂 DB** (پایین سمت چپ) را بزنید.
2.  دکمه **📤 Contribute Data** را بزنید.
3.  یک فایل (`contribution.json`) دانلود می‌شود و صفحه گیت‌هاب باز می‌شود.
4.  فایل دانلود شده را داخل کادر متن گیت‌هاب بکشید و رها کنید (Drag & Drop) و دکمه ارسال را بزنید.

---

### ❓ سوالات متداول

**آیا اکانت من بن می‌شود؟**
خیر. این ابزار فقط اطلاعاتی را می‌خواند که خود توییتر به مرورگر شما می‌فرستد (Public API). هیچ کار غیرقانونی یا هکی انجام نمی‌شود.

**چرا گاهی اوقات می‌نویسد "Unknown"؟**
اگر کاربر لوکیشن خود را کاملاً بسته باشد یا اکانت بسیار امنیتی باشد، ممکن است دیتایی پیدا نشود. اما همچنان ID ثابت و تاریخ ساخت نمایش داده می‌شود.

---

**توسعه داده شده با ❤️ برای شفافیت و امنیت.**
